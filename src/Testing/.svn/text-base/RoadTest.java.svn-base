package Testing;


import Data.Car;
import Data.Road;

/**
 * Test class
 * @author Tom Pepels
 */
public class RoadTest {

    /* public static void main(String[] args) {
        Road r = new Road(0, 1, 1000, 14, 10, 1);
        addCar(r, "Car 1", true);
        addTruck(r, "Truck 1", true);
        addCar(r, "Car 2", true);
        addCar(r, "Car 2", true);

        for (int i = 0; i < 100; i++) {
            r.update(0.5);
        }
    }

    public static void addCar(Road road, String name, boolean initial) {
        double length = 2.5;                       // Length of the car
        double dVel = road.getMaxSpeedCars();           // Desired velocity in m/s
        double timeHead = 1.5;                     // Recommended time headway
        double minSpace = 2;                       // Recommended gap between two vehicles
        double dAccel = 0.3;                       // Desired acceleration in m/s^2
        double decel = 3;                          // Comfortable deceleration rate

        road.addCar(new Car(length, dVel, minSpace, timeHead, dAccel, decel, name), initial);
    }

    public static void addTruck(Road road, String name, boolean initial) {
        double length = 10.5;                       // Length of the car
        double dVel = road.getMaxSpeedCars();           // Desired velocity in m/s
        double timeHead = 1.7;                     // Recommended time headway
        double minSpace = 2;                       // Recommended gap between two vehicles
        double dAccel = 0.3;                       // Desired acceleration in m/s^2
        double decel = 3;                          // Comfortable deceleration rate

        road.addCar(new Car(length, dVel, minSpace, timeHead, dAccel, decel, name), initial);
    } */
}
